# Policies & FAQ (source of truth)

Place **canonical** policy and FAQ content here. The prototype uses [`topics.json`](topics.json) (loaded by `/support` and the `lookup_policy` tool).

- The **Support** page (`/support` or similar) will render or summarize this content for humans.
- The agent’s **policy lookup** tool will return snippets from the **same** files—no invented policy text.

**Topics to cover (minimum for the prototype):** shipping times, returns/refunds (high level), password reset / account help—aligned with seeded scenarios and tools.

Add or rename files as you implement; keep this README in sync if the layout changes.
